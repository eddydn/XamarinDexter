<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

  <style name="AppTitle">
    <item name="android:textColor">@android:color/white</item>
    <item name="android:textSize">@dimen/title_font_size</item>
    <item name="android:textStyle">bold</item>
  </style>

  <style name="PermissionButton">
    <item name="android:background">@color/colorAccent</item>
    <item name="android:textColor">@android:color/white</item>
    <item name="android:textSize">@dimen/button_text_size</item>
    <item name="android:layout_marginTop">@dimen/permission_button_margin_vertical</item>
    <item name="android:layout_marginBottom">@dimen/permission_button_margin_vertical</item>
  </style>

  <style name="PermissionFeedback">
    <item name="android:textColor">@color/permission_unknown</item>
    <item name="android:gravity">center_vertical</item>
    <item name="android:layout_marginLeft">@dimen/feedback_margin_left</item>
    <item name="android:layout_marginTop">@dimen/permission_feedback_margin_vertical</item>
    <item name="android:layout_marginBottom">@dimen/permission_feedback_margin_vertical</item>
  </style>


</resources>
